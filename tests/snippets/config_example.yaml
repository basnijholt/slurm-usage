# Example configuration file for slurm-usage
# Place this file at one of:
#   - $XDG_CONFIG_HOME/slurm-usage/config.yaml
#   - ~/.config/slurm-usage/config.yaml
#   - /etc/slurm-usage/config.yaml

# Data directory configuration (optional)
# If not specified or null, defaults to ./data (relative to script)
# data_dir: null                  # Use default ./data directory
# data_dir: /var/lib/slurm-usage  # Explicit system directory

groups:
  applications:
    - alice
    - bob
  control:
    - charlie
    - david
  physical:
    - eve
    - frank
  quantum:
    - grace
    - henry
  software:
    - isabel
    - jack
  interns:
    - karen
    - larry
